export interface ServiceItem {
    id: string;
    title: string;
    impact: string;
    impactIcon: string;
    description: string;
}

export interface StatItem {
    value: string;
    label: string;
    description: string;
}

export interface TechItem {
    name: string;
    icon: string;
}
